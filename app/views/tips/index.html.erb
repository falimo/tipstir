<% @tips.each do |tip| %>
    <div class="booyah box col-10 offset-1">
      <h1><%= tip.title %></h1><br />
      <p><%= tip.description %></p><br />

      <% if tip.user.present? %>
        <small><%= tip.user.email %></small>
      <% end %>

    </div>
  <% end %>